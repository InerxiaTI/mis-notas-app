import 'package:flutter/material.dart';

class SemesterSubjectListWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [Text('data')],
    );
  }
}
